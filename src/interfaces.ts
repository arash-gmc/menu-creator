export interface Item {
  id: string;
  name: string;
  price: number;
  category: string;
  description: string;
}

export interface Restaurant {
  id: string;
  name: string;
  displayName: string;
}
