import React, { useContext } from "react";
import { UserContext } from "../Providers";
import CategoryIcon from "../components/CategoryIcon";

const MainPage = () => {
  return (
    <>
      <h1 className="text-red-500 font-bold">MainPage</h1>
    </>
  );
};

export default MainPage;
